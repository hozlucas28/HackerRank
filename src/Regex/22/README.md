# Challenge 22 - Positive lookahead

You have a test string $`S`$.

Write a regex that can match all occurrences of `o` followed immediately by `oo` in $`S`$.

### Solutions

- [TypeScript](solution.ts)
