import fn from "./solution.ts"
import { expect, test, describe } from "bun:test"

describe("Challenge <CHALLENGE NUMBER>: <CHALLENGE TITLE>", () => {
	test("Test <TEST NUMBER>", () => {
		const received = fn()
		const expected = "XXX"
		expect(received).toBe(expected)
	})
})
